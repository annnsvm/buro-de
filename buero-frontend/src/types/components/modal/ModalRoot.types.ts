import { GlobalModalState } from "./GlobalModalType.types";
import { UIModalStack } from "./UIModalType.types";

type ModalRootProps = {
  globalModal: GlobalModalState;
  uiModalStack: UIModalStack;
};

export type { ModalRootProps };
